import COLORS from "./colors";
import FONTS from "./fonts";
import SPACING from "./spacing";

const THEME = {
  colors: COLORS,

  fonts: FONTS,

  spacing: SPACING,

  radius: {
    sm: 10,

    md: 16,

    lg: 24,

    xl: 32,

    round: 999,
  },

  shadow: {
    shadowColor: "#000",

    shadowOffset: {
      width: 0,

      height: 8,
    },

    shadowOpacity: 0.2,

    shadowRadius: 12,

    elevation: 10,
  },
};

export default THEME;
